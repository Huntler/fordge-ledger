// Hand-written declaration for the generated parser.js (plain JS output from
// lezer-generator — see openscad.grammar and the "grammar:build" script).
// This repo doesn't enable allowJs, so TypeScript needs this file to type
// the import instead of reading parser.js directly.
import type { LRParser } from "@lezer/lr";

export declare const parser: LRParser;
